pipeline {
    agent any
    stages {
        stage("Maven Build") {
            when {
                branch 'dev'
            }
            steps {
                echo "welcome to jenkins.."
            }
        }
    }
}
