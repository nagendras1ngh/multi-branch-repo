pipeline {
    agent any
    stages {
        stage("Maven Build") {
            when {
                branch:'develop'
            }
            steps {
                echo "welcome to jenkins.."
            }
        }
    }
}
